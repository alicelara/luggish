<% set_active %>

<h1>List - <%= @list.name %></h1>


<table class="table table-striped">
    <tr>
      <th>List Name</th>
      <th>Gender</th>
      <th>Trip To</th>
      <th>From</th>
      <th>To</th>
    </tr>

    <tr>
      <td><%= @list.name %></td>
      <td><%= @list.sex %></td>
      <td><%= @list.location %></td>
      <td><%= @list.start_date %></td>
      <td><%= @list.end_date %></td>
    </tr>

  </table>

  <p>Prepare to pack!</p>
<% @list.items.each do |item| %>
  <ul>
 
      <div>item: <%= item.name %></div>
      <% @list.item_lists.each do |il| %>
       <div>qty: <%= il.quantity %></div>
       <div>notes: <%= il.note %></div>
       <% end %>

  </ul>
  <% end %>

  <%= link_to 'Edit', edit_list_path(@list) %> |
<%= link_to 'Back', lists_path %>
 </div>


